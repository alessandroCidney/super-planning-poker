import styled from 'styled-components'

import { motion } from 'motion/react'

export const StyledFormErrorMessage = styled(motion.div)`
  color: var(--theme-error-color);
  font-weight: 600;
  font-size: 1rem;
`